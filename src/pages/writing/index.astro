---
import Base from '../../layouts/Base.astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('writing', ({ data }) => !data.draft))
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());
const fmt = (d: Date) => d.toLocaleDateString('en-GB', { day: 'numeric', month: 'short', year: 'numeric' });
---
<Base title="Writing" description="Debug stories and notes from WLAN firmware and SoC bring-up.">
  <section class="block">
    <h2>writing</h2>
    <p class="lede">
      Long-form notes from bring-up and debug work. Each one is a mystery, a diagnosis and a fix.
      Client details are described by mechanism, not by register map.
    </p>
    <ul class="posts">
      {posts.map((p) => (
        <li>
          <time datetime={p.data.date.toISOString()}>{fmt(p.data.date)}</time>
          <div>
            <h3><a href={`/writing/${p.id}`}>{p.data.title}</a></h3>
            <p>{p.data.summary}</p>
            <div class="tags">{p.data.tags.map((t) => <span>#{t}</span>)}</div>
          </div>
        </li>
      ))}
    </ul>
  </section>
</Base>
